const { Given, When, Then } = require("@cucumber/cucumber");

Given("the user is on the create note page", function () {
  return "pending";
});

When("the user enters {string} as the note title", function (string) {
  return "pending";
});

When("the user enters {string} as the note content", function (string) {
  return "pending";
});

When("the user clicks the {string} button", function (string) {
  return "pending";
});

When("the user enters {string} as the note's tage", function (string) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Then("the note is successfully created", function () {
  return "pending";
});
