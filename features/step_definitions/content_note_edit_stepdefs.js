const { Given, When, Then } = require("@cucumber/cucumber");

Given("the user is revising an existing note G", function () {
  return "pending";
});

When("the user replaces the existing content with {string}", function (string) {
  return "pending";
});

When(
  "the user clicks the {string} button on note content edit",
  function (string) {
    return "pending";
  }
);

Then("the note G is successfully edited", function () {
  return "pending";
});
