const { Given, When, Then } = require("@cucumber/cucumber");

Given("the user is revising an existing note T", function () {
  return "pending";
});

When("the user replaces the existing title with {string}", function (string) {
  return "pending";
});

When(
  "the user clicks the {string} button on note title edit",
  function (string) {
    return "pending";
  }
);

Then("the note T is successfully edited", function () {
  return "pending";
});
