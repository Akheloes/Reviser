const { Given, When, Then } = require("@cucumber/cucumber");

Given("the user is revising an existing note C", function () {
  return "pending";
});

When("the user replaces the existing tag with {string}", function (string) {
  return "pending";
});

When("the user clicks the {string} button on note tag edit", function (string) {
  return "pending";
});

Then("the note C is successfully edited", function () {
  return "pending";
});
